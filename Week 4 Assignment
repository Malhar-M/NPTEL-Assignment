'''
#include <stdio.h>
// 1. Function Prototype (Tells the compiler about the function)
float calculateArea(float length, float width);
int main() {
    float l, w, area;  
    printf("Enter length and width: ");
    scanf("%f %f", &l, &w);
    // 2. Function Call (Passes 'l' and 'w' as arguments)
    area = calculateArea(l, w);
    printf("The area of the rectangle is: %.2f\n", area);
    return 0;
}
// 3. Function Definition (The actual logic)
float calculateArea(float length, float width) {
    return length * width; 
}
'''
